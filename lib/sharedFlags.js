"use strict"

// FILE USED FOR SHARED 
// FLAGS, BITSETS, ENUM-LIKE DATA, AND SO ON

const UID_Flags = {
	HOST : -1,
	NULL : -2
};

const CT_CONFIG = {
    INSTANCE_ID : 0,
    IS_HOST : false,
    IGNORE_REMOTE_INPUT : true,
    SINGLE_USER_PER_HOST_MODE_UID : -1 // -1 means not in this mode
};
